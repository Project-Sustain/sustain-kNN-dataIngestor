language: java

services:
  - mongodb

install:
  - ./gradlew build

script:
  - ./gradlew check

env:
  global:
    - GRADLE_OPTS=-Xmx256m

os:
  - linux

jdk:
  - oraclejdk9

notification:
  email: false

